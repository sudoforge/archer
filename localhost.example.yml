---
timezone:
  enabled: false
  hwclock: UTC
  zoneinfo: America/Los_Angeles

locale:
  enabled: false
  primary: en_US.UTF-8
  extra: []

hostapd:
  enabled: false
  ssid: archer-wifi
  passphrase: archerwifi
  interface: wlan0
  bridge: br0
  driver: nl80211
  channel: 7
  pairwise: CCMP
  service:
    started: false
    enabled: false

hostname:
  enabled: false
  name: archer
  aliases: []

initramfs:
  enabled: false
  preset:
    - linux
  packages: []
  hooks:
    - base
    - udev
    - autodetect
    - modconf
    - block
    - filesystems
    - keyboard
    - fsck

bootloader:
  systemd:
    enabled: false
    options:
      - root=/dev/sda2
      - quiet
      - rw
