---
package:
  netctl:
    installed: false

file:
  /etc/systemd/network/20-wired.network:
    exists: true
    owner: root
    group: root
    mode: "0644"
    filetype: file
  /etc/systemd/network/25-wireless.network:
    exists: true
    owner: root
    group: root
    mode: "0644"
    filetype: file

service:
  systemd-networkd:
    enabled: true
    running: false
  systemd-resolved:
    enabled: true
    running: true
